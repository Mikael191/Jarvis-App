<resources>
    <!-- Base application theme. -->
    <style name="Theme.JarvisAndroid" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/cyan_hologram</item>
        <item name="colorPrimaryVariant">@color/blue_dark</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/cyan_hologram</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/black</item>
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/black</item>
        <item name="android:textColor">@color/hologram_text</item>
    </style>

    <style name="HolographicButton" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="android:textColor">@color/cyan_hologram</item>
        <item name="strokeColor">@color/cyan_hologram</item>
        <item name="strokeWidth">2dp</item>
        <item name="backgroundTint">@color/transparent_black</item>
        <item name="cornerRadius">12dp</item>
    </style>
</resources>
